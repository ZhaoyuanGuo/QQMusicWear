# 更新日志

本项目的所有显著变更都记录在此文件中。

## 1.9.6 (2026-09-17)

### 修复（播放页针对亮色封面的适配）
- 背景失控：亮色封面（如高亮黄/白专辑图）自动降饱和（0.55）并加强压暗（scrim +0.16，上限 0.76）、暗角加深，整页不再被背景带偏；封面亮度由 32px 采样平均亮度（Rec.709）判定并缓存
- 歌名可读性：盘内顶部 scrim 0.45→0.55，歌名增加黑色文字阴影
- 副控件弧形分布：底部 音量/喜欢/音质/下载 四钮从横排贴底改为沿圆屏下缘弧线分布（±18°/±52°），不再被弧线裁切、不再挤进边缘手势热区
- 控件可见性：切歌钮/副控钮圆底由白色半透明改为深色圆底+细描边，任何封面色上都清晰；播放键增加深色底盘提供分离度
- 进度可读：顶部进度环缺口两侧显示 当前时间/总时长（替代系统时间，AOD 自动降亮度）
- 歌词页黑条：顶部/底部渐隐边缘由纯黑不透明改为黑 55% 半透明（峰值与背景压暗衔接），暖色/亮色封面上不再出现实心黑带吞歌词
- 歌词页胶囊：实况胶囊在歌词页不再显示（遮挡底部歌词行，且功能与页面内容重复）
- 我的页搜索栏：不再悬浮固定，随列表滚动（往下滑即跟随上移）；结果页保持顶部固定并自动恢复焦点/键盘
- 我的页语音按钮：麦克风加大为 34dp 深色圆底按钮（18dp 图标），更易发现和点中
- 歌单页操作行：下载全部小圆钮与播放全部胶囊并排一行（大胶囊+小圆钮），不再单独占一行
- 播放页布局错乱修复（根因）：ScreenScaffold 圆屏 contentPadding 把内容区压缩到 ~340×284，圆盘缩至 ~215px 且控制行（164dp）溢出——播放键被压缩、下一曲键宽度归零消失、底部钮行悬空在屏幕中部。播放页改为全屏沉浸约束（与背景/进度环一致），不应用 contentPadding；恢复圆盘上移 10dp
- 播放时间样式：由进度环缺口两端改为单行居中显示在缺口正中（0:06 / 3:53）
- 播放页构图再平衡：去除圆盘上移偏移（封面垂直居中不再偏高）、底部钮行下移贴底（bottom 8dp）
- 播放键不再遮挡歌手名：歌名/歌手块上移（盘顶 12%）、控制键组盘心下移 10dp，拉开垂直间距
- 音量交互重做：移除全屏音量面板（不再有"无法返回"问题）。表冠在播放页随时调音量；转表冠或点音量钮时，播放进度环临时切换为同款样式的音量弧（播放进度隐藏），底部胶囊显示 音量值 并提供触控 +/−，约 1.6 秒无操作自动消失

## 1.9.5 (2026-09-17)

### 优化（美术风格统一）
- 首页推荐大卡降饱和：高饱和纯色渐变改为深色低饱和渐变，与整体玻璃拟态风格统一
- 绿色纪律：品牌绿仅用于"正在发生"的动作/状态（播放、选中、已收藏等），VIP 角标、计数徽章、标题、装饰图标全部改为中性色
- 图标统一线性风格：重绘 21 个图标（1.5dp 描边 + 圆端点圆拐角），与系统图标风格一致
- 玻璃面板更通透：底色透明度 0.62→0.42、描边 0.08→0.14，去灰雾更锐利
- 字重层级拉开 + 播放页封面更透亮：盘内压暗 0.30→0.10，改为顶部局部渐变 scrim 保证歌名可读
- 形状收敛：卡片统一 20dp、小元素统一 12dp，胶囊造型保留
- 歌词页氛围：当前行 18sp 加粗 + 微光晕，非当前行 13sp 灰色，对比大幅增强；顶部/底部新增渐隐边缘

## 1.9.4 (2026-09-16)

### 优化
- 播放页：圆盘缩至 86% 屏宽居中，信息对齐微调
- 歌单页：「下载全部」改为列表头内小圆钮，节省纵向空间
- 「我的」页移除「返回推荐」入口

## 1.9.3 (2026-09-16)

### 修复
- 非手表环境（缺少 wearable 共享库，如部分模拟器）启动即闪退：AOD 观察改为检测共享库后条件注册
- 表冠音量误触防护：仅打开音量面板后表冠才调整音量，面板关闭时表冠不再影响音量

### 新增
- 全局触觉反馈：播放/暂停、切歌、收藏、下载、队列模式切换轻点确认；表冠音量步进刻度感；滑动换页/返回触发反馈（遵守系统"触摸振动"开关）
- 音质角标：播放页音质按钮直接显示当前音质（128 / HQ / SQ / HR），无需点开面板
- 生命感动效：播放时封面缓慢旋转（约 30 秒/圈）、播放键外圈光晕呼吸起伏；暂停即停并轻微变暗
- AOD（环境模式）支持：抬腕常亮时停止旋转/呼吸/歌词跟随动画并整体降亮度，省电护屏
- 方向化页面转场：歌词从右侧推入、播放队列从下方推入，其余页面按导航深度方向滑动，手势与视觉动线一致

### 优化
- 歌词：手动拖动浏览后自动跟随暂停 4 秒，不再"被拽回去"；当前行颜色/字号 220ms 平滑过渡，替代硬切换
- 「我的」页（收藏/歌单/搜索结果）全部换用 ScalingLazyColumn，圆屏缩放锚点与首页、队列页滚动体验一致
- 触摸命中区扩大：播放页副控制钮 28→42dp、切歌键 34→42dp、麦克风 16→32dp、清除历史按钮外扩（视觉尺寸不变）

## 1.9.2 (2026-09-16)

### 新增
- 语音搜索：搜索框麦克风直接说话搜索歌曲/歌手/歌单
- 歌词翻译：部分歌曲在原文下方显示译文
- 正在播放磁贴（Tile）：长按表盘快速进入播放页

### 修复
- 「我的」页 verticalScroll 与表冠滚动叠加导致的无限约束崩溃

## 1.9.1 (2026-09-16)

### 修复
- 首启音乐源下载三处缺陷（主线程网络、协程状态键、Ed25519 兼容性）导致的"所有镜像不可达"

## 1.9.0 (2026-09-15)

### 新增
- 音乐源插件化：JS 源外置（APK 无协议明文）+ Ed25519 签名校验 + 五级镜像容灾 + 凭据过期闭环
- 睡眠定时渐弱、点播入队、本地红心（未登录收藏）、听歌统计、表冠旋转（Rotary Input）
